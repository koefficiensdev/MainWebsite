export function confirmAction(message){
  return new Promise(resolve=>{const dialog=document.createElement('dialog');dialog.className='action-confirmation';dialog.setAttribute('aria-label','Művelet megerősítése');const text=document.createElement('p');text.textContent=message;const cancel=document.createElement('button'),accept=document.createElement('button');cancel.type=accept.type='button';cancel.textContent='Mégse';accept.textContent='Megerősítem';cancel.className='secondary';cancel.addEventListener('click',()=>dialog.close('cancel'));accept.addEventListener('click',()=>dialog.close('accept'));dialog.append(text,cancel,accept);dialog.addEventListener('close',()=>{const result=dialog.returnValue==='accept';dialog.remove();resolve(result);},{once:true});document.body.append(dialog);dialog.showModal();cancel.focus();});
}
